name: mensaApp
description: App zur Anzeige des Mensa-Menüs der KSWE.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.2
  intl: ^0.20.2
  shared_preferences: ^2.2.2
  logging: ^1.2.0
  http: ^1.5.0
  syncfusion_flutter_pdf: ^24.1.41

flutter:
  uses-material-design: false